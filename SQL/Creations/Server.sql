CREATE TABLE dbo.Server
(
	ServerID UNIQUEIDENTIFIER NOT NULL PRIMARY KEY,
	[Name] NVARCHAR(24) NOT NULL,
	Icon VARCHAR(256) NOT NULL,
	[Owner] UNIQUEIDENTIFIER NOT NULL FOREIGN KEY
		REFERENCES dbo.Runner(RunnerID)
			ON DELETE CASCADE
			ON UPDATE CASCADE
);